# Archived Files

这个文件夹用于保存旧测试代码和历史草稿。

当前主流程代码不在这里。请优先使用：

```text
arduino/dual_screen_state_loop/dual_screen_state_loop.ino
arduino/dlight_flask_serial/dlight_flask_serial.ino
backend/sprout_server.py
```

## legacy_2026-05-26

这批文件已经被新主程序吸收或替代：

- `Dlight524/`：早期 DLight + Flask 串口测试。
- `dlight_m5_plant_state/`：早期光照控制彩屏小芽测试，核心逻辑已合入 `dlight_flask_serial.ino`。
- `imu_motion_test/`：早期 DLight + IMU 移动检测测试，核心逻辑已合入 `dlight_flask_serial.ino`。
- `screen_display_test_525/`：早期 OLED 单屏动画测试。
- `screen_display_test_525_v2/`：早期 OLED / 屏幕显示测试变体。
- `root_dlight_m5_plant_state.ino`：根目录里的临时副本。
